global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - "/etc/prometheus/rules/*.rules.yml"

scrape_configs:
  - job_name: "neuro-pipeline-central"
    static_configs:
      - targets: ["host.docker.internal:9090"]
    metrics_path: /metrics
